<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.renren.modules.policy.dao.PolicyTypeDao">

    <!-- 查询地区 -->
    <select id="queryTypeName"  resultType="java.util.Map">
        SELECT id, type as label, type as value FROM policytype WHERE type  !=""
    </select>

</mapper>